#ifndef SILLYRABBIT_UTILS_FILEUTILS_HPP
#define SILLYRABBIT_UTILS_FILEUTILS_HPP

#include <string>
#include <cstring>

namespace sr { namespace utils {

    std::string readFile(const char* filepath);

} }

#endif // EYAX_UTILS_FILEUTILS_HPP
